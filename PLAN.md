# 이미지 학습 자동화

**Capstone 트랙**: 트랙 B — 자동화 Agent · Workflow
**구현에 쓸 수 있는 시간**: 코칭 전후·저녁 + 다음 날 발표 전까지 (실질 6~8시간)

## ① 문제 정의 — 지금 무엇이 불편한가
- 누가 / 언제 / 어떤 일에서: 본인(프로젝트 담당자)이 생산 현장 이미지 OK/NG 분류 모델을 운영 중이며, 매일 새 이미지가 유입되고 모델 판정과 사람 판정이 다른 오분류 이미지가 지속 발생함
- 줄이는 비용 (반복 / 대기 / 검증 중 하나): 반복 — 오분류 데이터 정리, 재학습 실행, 신구 모델 비교를 매번 손으로 함
- 지금 걸리는 시간·횟수: 재학습할 만큼 오분류 데이터가 쌓이는 주기가 약 일주일. 그 이후 데이터 정리 → 재학습 → 비교 → 교체 판단까지 전 과정이 수작업

## ② AI 에이전트에게 맡길 부분
- 에이전트가 대신할 일: 신규 이미지 추론, 오분류·저confidence 이미지 자동 수집, 재학습 조건 판단·실행, 신구 모델 비교·리포트 생성, 승인 파일 유무로 교체 게이트 관리
- 사람이 계속 할 일: Review Queue 이미지 최종 OK/NG 라벨링, model_v2 운영 적용 최종 승인
- 쓸 도구 (서브에이전트·MCP·스킬·hook·병렬 중 필요한 것만): MVP는 서브에이전트 분리 없이 Main Agent + 단계별 스크립트(Skill) 구조로 시작 (4개 서브에이전트 분리는 "되면 좋은"으로 미룸). 승인 게이트는 hook보다 스크립트 자체 검증(approve.txt 없으면 exit)으로 구현 — 별도 하네스 설정 없이도 명확히 동작하기 때문

## ③ 범위 — 반드시 / 되면 좋은 / 안 하는 것
- 반드시 (이게 안 되면 실패):
  1. 샘플 OK/NG 이미지로 초기 모델 1개 학습
  2. 신규 이미지 추론 + confidence 낮은/오분류 이미지를 Review Queue에 자동 수집
  3. 사람이 Review Queue를 보고 OK/NG 재판정 (화면일 필요는 없음, 간단한 방식)
  4. 일정량 쌓이면 에이전트가 재학습 실행 → 신규 모델 생성
  5. 기존 모델 vs 신규 모델을 고정 검증셋으로 비교해 Accuracy 등 지표 산출 → Markdown 리포트 생성
  6. 리포트 기준 "적용 가능/불가"를 에이전트가 판단하되, 최종 교체는 사람 승인이 있어야 실행됨
- 되면 좋은:
  - Main/Data/Training/Evaluation 4개 서브에이전트로 명확히 분리
  - 모델 버전 메타데이터(학습일, 데이터 수, 평가 결과 등) 체계적 관리
  - 반복 루프 자동 스케줄링
- 이번엔 안 하는 것:
  - 실제 웹 화면(UI) 구축
  - Precision/Recall 등 세부 지표 전부, NG별 오분류 상세 분석
  - 롤백 자동화 (버전 되돌리기는 수동)

## ④ Phase 분할과 각 Phase 완료 기준
1. 샘플 데이터 생성 + 초기 모델(model_v1) 학습 — 완료: 샘플 OK/NG 이미지 생성 스크립트 실행 후 데이터 폴더에 이미지 파일 생성, `model_v1` 파일 저장됨 (실습 환경에는 기존 운영 모델이 없으므로 model_v1을 그 대역으로 새로 학습)
2. 추론 + Review Queue 자동 수집 — 완료: 신규 이미지 배치 추론 실행 후 `review_queue.json`(오분류·저confidence 이미지 목록)이 1건 이상 채워짐
3. 사람 판정 반영 + 재학습 → model_v2 생성 — 완료: Review Queue 항목에 OK/NG 라벨을 채운 뒤 재학습 스크립트 실행 시 `model_v2` 파일이 새로 생성됨
4. 모델 비교 + 리포트 생성 + 승인 게이트 — 완료: 비교 스크립트 실행 시 `report.md`에 Accuracy 등 지표가 표로 생성되고, 승인 파일(예: `approve.txt`) 없이는 모델 교체 스크립트가 실행되지 않음(거부 케이스로 확인 가능)

## ⑤ 완료를 판정할 방법 (검증 게이트)
- 기계가 판정하는 것: 각 Phase 산출물 파일 존재 여부(`model_v1`/`model_v2`, `review_queue.json`, `report.md`), `report.md`에 Accuracy 등 지표 포함 여부, `approve.txt` 없이 교체 스크립트 실행 시 실패(거부)하는지
- 사람이 눈으로 보는 것: `report.md` 지표가 실제로 타당해 보이는지, Review Queue에 모인 이미지가 적절히 선별됐는지
- 내가 직접 승인할 지점: `model_v2`를 운영 모델로 교체할지 (`approve.txt` 생성 여부로 표현)

## GitHub 저장소 URL
https://github.com/one2seven/CNN_Agent

## 1:1 코칭에서 가장 묻고 싶은 것
실제 업무 환경에서는 Claude가 구동되는 네트워크와 실제 이미지·분류 정보가 있는 네트워크가 분리되어 있는 경우가 많다. 이렇게 망이 분리된 상황에서 Claude(에이전트)에게 해당 데이터를 전달하는 방법은 무엇인가?

---
(아래는 폼에 넣지 않는 작업 메모)

## 되돌아갈 지점과 데이터
- 되돌리는 방법: git 저장소 초기화 완료, PLAN.md 확정 상태로 첫 커밋(root-commit) 남김 — 이후 문제가 생기면 이 커밋으로 되돌아감
- 실데이터·자격증명 없이 되게 만드는 방법 (샘플로 대체할 부분): 실제 생산 현장 이미지 대신 도형(원/사각형) 기반 합성 이미지를 PIL/OpenCV로 직접 생성. OK는 깨끗한 도형, NG는 흠집·얼룩·찌그러짐을 랜덤 추가. 난이도를 조절해 애매한 케이스(저confidence)도 의도적으로 포함

## 고칠 곳 (4단계 반박 결과)
1. [범위] Phase별 시간 배분이 타이트함. ML 환경 셋업에 시간이 샐 위험 → 가벼운 모델(소형 CNN 또는 scikit-learn 분류기)로 미리 못박아두는 것을 권장.
2. [④ Phase 2 완료 기준] "review_queue.json 1건 이상"이 보장되려면 합성 데이터 생성 시 애매한(저confidence 유발) 케이스를 의도적으로 일정 비율 포함해야 함. 현재 데이터 메모에는 방향만 있고 Phase 2 완료 조건과 명시적으로 연결돼 있지 않음.
3. [검증 불가 조건] 없음 — ④의 완료 기준은 전부 기계 판정 가능.
4. [승인 지점 누락] 없음 — 모델 교체 지점에 approve.txt 게이트 존재.

<!-- 기획 시작 09:06 -->
